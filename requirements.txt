# requirements.txt

python-dotenv>=1.0       # for loading env vars from .env files
requests>=2.28            # for HTTP requests to APIs
pandas>=2.0                # for DataFrame results
numpy>=1.25                # numeric operations
pytest>=7.0                # for running tests
responses          # to mock HTTP calls in tests
yahoo_oauth>=0.5.0          # for Yahoo OAuth2 authentication 
yahoo-fantasy-api>=2.0.0   # for accessing Yahoo Fantasy Sports API
pytest-mock>=3.10.0        # for mocking in pytest
